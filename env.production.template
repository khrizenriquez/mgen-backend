# ===================================================================
# SISTEMA DE DONACIONES - CONFIGURACIÓN DE PRODUCCIÓN (Railway)
# ===================================================================
# Copia este archivo y configura las variables en Railway Variables
# NO commiteas valores reales - solo placeholders para documentación

# ===================================================================
# BASE DE DATOS (Railway PostgreSQL)
# ===================================================================
DATABASE_URL=${DATABASE_URL}
POSTGRES_USER=${PGUSER}
POSTGRES_PASSWORD=${PGPASSWORD}
POSTGRES_DB=${PGDATABASE}
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}

# Database Connection Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
SQL_ECHO=false

# ===================================================================
# RABBITMQ (Railway RabbitMQ)
# ===================================================================
RABBITMQ_URL=${RABBITMQ_URL}
RABBITMQ_HOST=${RABBITMQ_HOST}
RABBITMQ_PORT=5672
RABBITMQ_USER=${RABBITMQ_USER}
RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
RABBITMQ_VHOST=/

# ===================================================================
# CONFIGURACIÓN DE APLICACIÓN
# ===================================================================
ENVIRONMENT=production
DEBUG=false
SECRET_KEY=${SECRET_KEY}
JWT_SECRET_KEY=${JWT_SECRET_KEY}
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
SERVICE_NAME=donations-api
VERSION=1.0.0

# ===================================================================
# RATE LIMITING
# ===================================================================
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# ===================================================================
# CONFIGURACIÓN DE ORGANIZACIÓN POR DEFECTO
# ===================================================================
DEFAULT_ORGANIZATION_NAME=Más Generosidad Guatemala
DEFAULT_ORGANIZATION_DESCRIPTION=Organización principal de donaciones en Guatemala
DEFAULT_ORGANIZATION_EMAIL=contacto@masgenerosidad.org

# ===================================================================
# CONFIGURACIÓN DE EMAIL (SendGrid)
# ===================================================================
# IMPORTANTE: Obtén tu API key desde https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=${SENDGRID_API_KEY}
FROM_EMAIL=noreply@masgenerosidad.org
FROM_NAME=Más Generosidad Guatemala

# ===================================================================
# URLS DE FRONTEND Y API
# ===================================================================
FRONTEND_URL=https://masgenerosidad.org
API_BASE_URL=https://api.masgenerosidad.org

# ===================================================================
# CONFIGURACIÓN DE LOGGING
# ===================================================================
LOG_LEVEL=INFO
LOG_LEVEL_NUM=20

# ===================================================================
# CONFIGURACIÓN DE CORS Y SEGURIDAD
# ===================================================================
ALLOWED_HOSTS=masgenerosidad.org,www.masgenerosidad.org,api.masgenerosidad.org
ALLOWED_ORIGINS=https://masgenerosidad.org,https://www.masgenerosidad.org,https://app.masgenerosidad.org
CSRF_SECRET_KEY=${CSRF_SECRET_KEY}

# ===================================================================
# MONITORING (Opcional - Apitally)
# ===================================================================
# APITALLY_CLIENT_ID=${APITALLY_CLIENT_ID}
# APITALLY_ENV=prod

# ===================================================================
# INSTRUCTIONS PARA RAILWAY
# ===================================================================
# Variables que necesitas configurar en Railway Variables:
#
# 1. DATABASE_URL (automático cuando conectas PostgreSQL)
# 2. RABBITMQ_URL (automático cuando conectas RabbitMQ)
# 3. SECRET_KEY (genera una clave segura aleatoria)
# 4. JWT_SECRET_KEY (genera una clave segura aleatoria diferente)
# 5. SENDGRID_API_KEY (obtén de SendGrid dashboard)
# 6. CSRF_SECRET_KEY (genera una clave segura aleatoria)
#
# Variables opcionales:
# - APITALLY_CLIENT_ID (para monitoring avanzado)
#
# Una vez configuradas estas variables, el despliegue debería funcionar correctamente.
